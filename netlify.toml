# netlify.toml

[build]
  command = "npm run build"
  publish = "dist"

# Remove all [[redirects]] blocks. 
# Your App.jsx is now calling /.netlify/functions/research directly,
# so you no longer need the /api-anthropic or /api-gemini proxies.